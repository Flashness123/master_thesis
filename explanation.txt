Shapes:

Lance dataset
─────────────────────────────────────
pixels              action IDs
[4,3,224,160]       [4,4]

              ↓ transforms

LeWM input
─────────────────────────────────────
pixels              one-hot actions
[4,3,224,224]       [4,24]

              ↓ DataLoader

batch
─────────────────────────────────────
pixels              actions
[B,4,3,224,224]     [B,4,24]

              ↓ JEPA.encode()

latent states        action latents
[B,4,192]           [B,4,192]

              ↓ slicing

context
[z0,z1,z2]
[B,3,192]

actions
[A0,A1,A2]
[B,3,192]

targets
[z1,z2,z3]
[B,3,192]

              ↓ ARPredictor

predictions
[ẑ1,ẑ2,ẑ3]
[B,3,192]

              ↓

MSE(predictions, targets)
+
0.09 × SIGReg(z0,z1,z2,z3)

              ↓

backpropagation
              ↓

update:
ViT
projector
action encoder
predictor
prediction projector

How to use:
uv run python -m master_thesis.training.lewm --config-name train/arc_lewm trainer.max_epochs=1 # the trainer.max_epochs=1 are direct edits in the config




++++++++++++++++++++++++++++++
How to run:
PPO training on lewm:
uv run python -m master_thesis.training.ppo \
  --input lewm \
  --lewm-run "arc/ls20_3060ti_20260913_234616/lewm_ppo_round1" \
  --lewm-weights weights.pt \
  --game ls20-9607627b \
  --run-name "arc/ls20_3060ti_20260913_234616/ppo_lewm_round1_$(date +%Y%m%d_%H%M%S)" \
  --steps 204800 \
  --max-steps 500 \
  --history 3 \
  --eval-every 20480 \
  --eval-episodes 10 \
  --seed 42 \
  --device cuda